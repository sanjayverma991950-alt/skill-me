# SkillMe Frontend Environment Configuration
# ------------------------------------------
# Set this to your deployed backend URL if you want the frontend to connect directly to the cloud backend (e.g. Render).
# Example: VITE_API_URL=https://skill-me-2.onrender.com
# For local development with Vite proxy to http://localhost:8000, leave it empty.
VITE_API_URL=
